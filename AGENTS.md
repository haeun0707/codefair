# 다시만나 AI 개발 규칙

이 저장소는 한국 중학교 3학년 2인 팀이 **2026년 제8회 한국코드페어 해커톤 중등부**에 출품할 교육용 AI/SW 프로젝트 **다시만나 AI**이다.

## 제품 목표

실종 반려동물 보호자가 등록한 여러 기준 사진과 특징을 시민의 목격 제보와 비교해, 먼저 확인할 제보 순서와 수색 참고 범위를 제안한다.

1. 반려동물 기준 사진 1~5장과 특징 등록
2. 목격 사진·시각·위치·관찰 특징 여러 건 입력
3. 사진 색 분포·윤곽·시각 패턴과 털 색·얼굴 무늬·귀·체형·꼬리 특징 비교
4. 일치·불일치·확인 불가 근거 표시
5. 흐림·가림·정보 부족 시 `판단 보류` 및 추가 사진 요청
6. 시간·직선거리로 이동 가능성 확인
7. 보호자가 확인한 제보만 경로와 다음 수색 범위 계산에 사용

## 안전 및 표현 원칙

- AI 결과를 동일 동물 확정 또는 동일 동물일 확률로 표현하지 않는다.
- 점수는 반드시 **검토 우선도** 또는 **단서 유사도**라고 표시한다.
- 사진이 흐리거나 가려졌거나 특징이 부족하면 억지로 판정하지 않고 `판단 보류`한다.
- 보호자 또는 구조 담당자의 원본 확인 없이 제보를 경로에 자동 포함하지 않는다.
- 경로와 예상 수색 범위는 도로·지형·동물 행동을 반영하지 않은 참고값이라고 표시한다.
- 실제 집 주소, 개인 연락처, 사람 얼굴, 정확한 생활 동선을 저장소에 추가하지 않는다.
- 업로드 자료를 서버 파일이나 저장소에 자동 저장하지 않는다.
- 대회 시연은 가상 반려동물·가상 위치 또는 허가받은 자료를 사용한다.
- 연락처와 정확한 주소를 공개 게시물에 그대로 노출하도록 유도하지 않는다.
- 실패 시 오류를 숨기지 말고 한국어 오류 메시지와 다음 행동을 안내한다.
- `except Exception: pass`처럼 오류를 무시하는 코드를 작성하지 않는다.

## 기술 원칙

- Python 3.11을 기준으로 한다.
- UI는 Streamlit, 이미지 처리는 OpenCV headless를 사용한다.
- 첫 시제품은 네트워크, 모델 다운로드, 외부 API 없이 CPU에서 실행한다.
- 핵심 비교·이동 로직을 Streamlit UI와 분리한다.
- 기준 사진은 여러 장의 특징을 평균해 조명과 각도 차이를 줄인다.
- 동물 자동 분할 모델이 없으므로 사용자가 동물이 크게 보이도록 자른 사진을 사용하게 안내한다.
- 테스트에서는 네트워크 연결과 모델 다운로드를 금지한다.
- 실제 사진, 생성 결과, 캐시, 개인정보를 Git에 포함하지 않는다.
- 실제 측정하지 않은 정확도·성공률·검증 결과를 만들어내지 않는다.

## 현재 제외 범위

- 사람 확인 없는 실종 동물 자동 확정
- 얼굴 또는 사람 신원 인식
- 실시간 CCTV 연결 및 무단 CCTV 접근
- 연락처 자동 수집·공개 또는 위치 실시간 공유
- 지자체·경찰·동물등록 데이터베이스 연결
- 실제 도로망·지형·동물 행동 모델 기반의 정밀 경로 예측
- 새로운 AI 모델 학습
- 구조 성공률 또는 동일 개체 확률 주장

## 작업 완료 조건

- `pytest -q`가 통과한다.
- `streamlit run app.py`로 실행할 수 있다.
- 가상 데모에서 일치 제보, 흐린 제보, 이동 불가능 제보, 특징 불일치 제보를 확인할 수 있다.
- 실제 JPG/PNG를 등록하는 수동 입력 흐름이 작동한다.
- 판단 보류·추가 자료 요청·사람의 경로 포함 확인이 작동한다.
- README와 TASKS.md를 현재 상태에 맞게 업데이트한다.
- 완료 보고에 변경 파일, 실행 명령, 테스트 결과, 실행하지 못한 부분, 한계, 다음 작업을 포함한다.

## 대회 제출물 기준

- 작품 요약서는 A4 1장 이내, 본문 12pt 기준으로 준비한다.
- 작품설명서는 표지와 출처를 제외하고 A4 15장 이내, 글씨 10~15pt, 검은색을 기본으로 준비한다.
- 인용 자료의 출처는 설명서 마지막에 반드시 별도 표기한다.
- 소스코드나 상세 설계가 15장을 넘으면 별첨 파일로 분리한다.
- 현재 작품은 시제품 완성 시 **순수 SW 작품** 구조로 설명한다.
- 설명서에는 개발 배경·필요성, 목적, 유사 제품과 차별점, 설계, 제작 과정, 테스트 목적·방법·예상 결과·실제 결과·분석, 결론, 개선사항을 포함한다.
- 참가신청서의 학생·보호자 개인정보와 동의 내용은 코드 저장소에 기록하지 않는다.

## 변경 전후 절차

- 코드 수정 전에 저장소를 살펴보고 구현 계획과 위험 요소를 제시한다.
- 기존 사용자 변경을 보존하고 관련 없는 파일을 수정하지 않는다.
- UI만 만들고 테스트를 생략하지 않는다.
- 실제 노트북에서 설치, OpenCV, 지도 표시, 처리 속도를 마지막으로 검증한다.
