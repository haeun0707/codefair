# MoaView 개발 규칙

이 저장소는 한국 중학교 3학년 2인 팀이 **2026년 제8회 한국코드페어 해커톤 중등부**에 출품할 교육용 AI/SW 프로젝트 **MoaView(모아뷰)** 이다.

## 최우선 목표

첫 번째 프로토타입은 다음 흐름만 안정적으로 완성한다.

1. JPG/PNG 이미지 1~3개 업로드
2. 차량 탐지
3. 여러 차량 중 목표 차량을 사용자가 직접 선택
4. 선택 차량의 화질 지표 비교
5. 원본 crop과 보정 참고 영상을 나란히 표시

한 단계의 구현과 테스트가 완료되기 전에는 다음 단계로 범위를 넓히지 않는다.

## 안전 및 표현 원칙

- 보정 결과는 항상 **보정 참고 영상**이라고 표시한다.
- 원본에 없거나 보이지 않는 정보를 복원했다고 표현하지 않는다.
- AI 결과는 차량 확정이나 동일 차량 판정이 아닌 **검토 보조 결과**로 설명한다.
- 실제 개인정보, 실제 차량번호, 실제 CCTV 영상, 사람 얼굴을 저장소에 추가하지 않는다.
- 시연 자료는 가상 번호판, 모형 자동차 또는 허가받은 촬영 자료를 사용한다.
- 차량이 여러 대면 가장 큰 차량 등을 자동으로 목표 차량으로 확정하지 않고 사용자가 직접 고르게 한다.
- 실패 시 오류를 숨기지 말고 한국어 오류 메시지와 다음 행동을 안내한다.
- `except Exception: pass`처럼 오류를 무시하는 코드를 작성하지 않는다.

## 기술 원칙

- Python 3.11을 기준으로 한다.
- UI는 Streamlit, 영상 처리는 OpenCV headless, 탐지는 경량 Ultralytics YOLO를 사용한다.
- YOLO 모델은 필요할 때만 lazy-load한다.
- 모델을 사용할 수 없는 환경에서도 전체 UI 흐름을 확인할 수 있는 mock mode를 제공한다.
- 핵심 로직을 Streamlit UI 코드와 분리한다.
- detector는 테스트에서 fake 구현으로 교체할 수 있게 설계한다.
- 테스트에서는 네트워크 연결과 모델 다운로드를 금지한다.
- 모델 파일, 실제 영상, 생성 결과, 캐시, 개인정보를 Git에 포함하지 않는다.
- CPU 실행을 기본으로 고려한다.

## 현재 제외 범위

- 번호판 OCR
- 자동 다중 CCTV 차량 재식별
- 차량 소유주 조회
- 경찰 데이터베이스 연결
- 얼굴 인식
- 실시간 CCTV 연결
- LLM 보고서
- 보이지 않는 번호판 문자 복원
- 새로운 AI 모델 학습

## 작업 완료 조건

- `pytest -q`가 통과한다.
- `streamlit run app.py`로 실행할 수 있다.
- mock mode에서 이미지 1~3개의 전체 흐름을 확인할 수 있다.
- 실제 YOLO 모드는 로컬 모델이 있거나 모델 사용 조건이 충족될 때 작동한다.
- README와 TASKS.md를 현재 상태에 맞게 업데이트한다.
- 완료 보고에 변경 파일, 실행 명령, 테스트 결과, 실행하지 못한 부분, 한계, 다음 작업을 포함한다.

## 대회 제출물 기준

- 작품 요약서는 A4 1장 이내, 본문 12pt 기준으로 준비한다.
- 작품설명서는 표지와 출처를 제외하고 A4 15장 이내, 글씨 10~15pt, 검은색을 기본으로 준비한다.
- 인용 자료의 출처는 설명서 마지막에 반드시 별도 표기한다.
- 소스코드나 상세 설계가 15장을 넘으면 별첨 파일로 분리한다.
- 현재 작품은 **시제품 완성 시 순수 SW 작품** 구조로 설명한다.
- 작품설명서에는 개발 배경·필요성, 목적, 유사 제품과 차별점, 설계, 제작 과정, 테스트 목적·방법·예상 결과·실제 결과·분석, 결론, 개선사항을 포함한다.
- 실제 측정하지 않은 수치나 테스트 결과를 만들어내지 않는다.
- 참가신청서의 학생·보호자 개인정보와 동의 내용은 코드 저장소에 기록하지 않는다.

## 변경 전후 절차

- 코드 수정 전에 저장소를 살펴보고 구현 계획과 위험 요소를 제시한다.
- 기존 사용자 변경을 보존하고 관련 없는 파일을 수정하지 않는다.
- UI만 만들고 테스트를 생략하지 않는다.
- 실제 노트북에서 설치, 모델 로딩, OpenCV, 코덱, 처리 속도를 마지막으로 검증한다.
