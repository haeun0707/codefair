# MoaView 프로젝트 브리프

## 프로젝트 개요

MoaView는 여러 CCTV 또는 관찰 이미지에서 차량을 검토할 때, 차량 후보를 탐지하고 사람이 목표 차량을 선택한 뒤 화질과 보정 참고 영상을 비교하도록 돕는 교육용 AI/SW 프로젝트다.

대상은 한국 중학교 3학년 2인 팀이며, **2026년 제8회 한국코드페어 해커톤 중등부** 출품을 목표로 한다. 예선 제출 전 저장소는 비공개로 운영하는 것을 권장한다.

## 공식 제출 서식에서 확인한 기준

- 작품 요약서: A4 1장 이내, 12pt. 작품 주제, 제안하는 해법과 근거, 기존 해법과의 차이, 결과를 담는다.
- 작품설명서: 표지와 출처를 제외하고 A4 15장 이내, 글씨 10~15pt, 검은색 기본. 출처 표기는 마지막 붙임 자료로 반드시 넣는다.
- 소스코드와 상세 설계 도면처럼 본문에 넣기 어려운 자료는 별첨 파일로 제출할 수 있다.
- 현재 MoaView는 `시제품 완성 시 Physical Computing 혹은 순수 SW 작품` 중 순수 SW 작품 구조를 따른다.
- 설명서 핵심 순서: 개발 배경 및 필요성 → 개발 목적과 해법 → 유사 제품 및 차별점 → 설계와 제작 → 테스트 목적·방법·예상/실제 결과·분석 → 결론과 추후 개선사항.
- 참가신청서에는 중등부 선택, 학생 2명과 각 보호자 정보, 개인정보·영상촬영 동의가 필요하지만 해당 개인정보는 저장소에 저장하지 않는다.

## 해결하려는 문제

촬영 위치와 환경이 다른 여러 이미지에는 가림, 흐림, 밝기 차이, 대비 차이가 생길 수 있다. MoaView는 AI가 차량을 확정하거나 보이지 않는 정보를 복원하는 시스템이 아니다. 사용자가 차량 후보와 원본 근거를 검토하기 쉽게 정리하는 보조 도구다.

## 첫 번째 MVP 범위

### 1단계: 자료 입력

- JPG/PNG 이미지 1~3개 업로드
- 각 이미지에 카메라 이름, 촬영 시각, 이동 방향 입력

### 2단계: 차량 탐지와 선택

- 경량 YOLO 모델을 lazy-load하여 차량 후보 탐지
- 후보별 crop, 종류, 객체탐지 확신도 표시
- 후보가 여러 대면 사용자가 목표 차량을 직접 선택
- 모델 실행이 어려운 환경을 위한 mock mode 제공

### 3단계: 화질 비교

선택한 차량 crop마다 다음 값을 표시한다.

- 객체탐지 확신도
- 선명도
- 평균 밝기
- 대비

원본 차량 crop과 OpenCV로 처리한 **보정 참고 영상**을 나란히 표시한다.

### 4단계: 초기 결과

- 가장 선명한 근거 영상 제시
- 선택 근거를 선명도와 탐지 확신도 등 관찰 가능한 지표로 설명
- 다음 주의 문구 표시

> 보정 영상은 확인을 돕기 위한 참고자료이며, 원본에 존재하지 않는 정보를 복원하지 않습니다.

## 기술 조건

- Python 3.11
- Streamlit
- OpenCV headless
- Ultralytics YOLO 경량 모델
- CPU 우선 실행
- UI와 핵심 로직 분리
- fake detector로 교체 가능한 구조
- pytest 단위 테스트
- 테스트 중 네트워크와 모델 다운로드 금지

## 개인정보와 데이터 원칙

- 모델 파일, 실제 영상, 생성 결과, 캐시를 Git에 포함하지 않는다.
- 실제 번호판, 사람 얼굴, 개인정보를 저장소에 포함하지 않는다.
- 시연 입력은 로컬 `inputs` 폴더에만 보관하고 Git에서 제외한다.
- 가상 번호판, 모형 자동차, 허가받은 촬영 자료를 사용한다.

## 단계별 확장 계획

1. 이미지 업로드, 차량 탐지, crop, 화질 지표, 보정 참고 영상
2. 차종·색상·시간·방향의 단서 교차검증
3. 사람의 최종 확인과 근거 기반 보고서
4. 시연 모드, 오류 대응, 발표 영상용 화면
5. 선택 사항: 번호판 영역 수동 선택과 보수적 OCR 후보

자동 다중 CCTV 재식별과 한국 번호판 OCR은 난도가 높은 별도 범위로 취급한다.

## 첫 MVP 완료 기준

- `pytest -q` 통과
- `streamlit run app.py` 실행 가능
- mock mode에서 이미지 1~3개의 전체 흐름 확인 가능
- 조건이 충족될 때 실제 YOLO 모드 작동
- Windows 실행 및 오류 해결 방법을 README에 기록
- TASKS.md 상태 업데이트

## 결과 검토 체크리스트

완료 보고에는 다음이 포함되어야 한다.

- 수정한 파일 목록
- 실제 실행한 명령
- pytest 결과
- 실행하지 못한 부분
- 남아 있는 한계
- 다음 작업 제안

다음 위험 신호를 확인한다.

- 오류를 조용히 무시하는 코드
- 테스트 없이 UI만 구현
- 모델 파일, 실제 영상, 번호판 샘플을 Git에 추가
- 보정 결과를 복원 영상이나 복원된 번호판으로 표현
- 점수를 동일 차량일 확률로 표현
- 여러 후보 중 가장 큰 차량을 자동 확정
- YOLO 실패 시 앱 전체 종료

## Windows 로컬 검증 명령

```powershell
python -m venv .venv
.venv\Scripts\activate
python -m pip install --upgrade pip
pip install -r requirements.txt
pytest -q
streamlit run app.py
```

`app.py`가 만들어지기 전 기존 스타터가 있다면 다음처럼 실행한다.

```powershell
python moaview_v0.py
```

최종 시연 전에는 실제 노트북에서 모델 준비, OpenCV 설치, 영상 코덱, CPU 처리 속도를 다시 확인한다.

## 권장 저장소 운영

- 저장소 이름 예시: `moaview-kcf-2026`
- 예선 제출 전에는 Private 저장소 권장
- 저장소 루트에서 프로젝트 문서와 코드가 바로 보이게 구성
- 초기 커밋 메시지 예시: `chore: initialize MoaView Codex project`
