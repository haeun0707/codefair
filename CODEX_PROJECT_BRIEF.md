# 다시만나 AI 프로젝트 개요

## 문제

실종 반려동물의 목격 제보는 지역 커뮤니티와 SNS를 통해 여러 건 들어올 수 있다. 그러나 촬영 각도·조명·화질이 다르고 비슷한 동물도 많아 보호자가 모든 제보를 직접 비교하기 어렵다. 잘못된 제보를 따라가는 동안 더 중요한 목격을 놓칠 수도 있다.

## 해결 아이디어

보호자가 등록한 여러 사진과 털 색, 얼굴 무늬, 귀 모양, 체형, 꼬리 특징을 목격 제보와 비교한다. 결과는 동일 동물 판정이 아니라 **먼저 확인할 제보 순서**이며, 일치·불일치·확인 불가 근거를 함께 보여준다.

사진이 흐리거나 동물이 일부 가려졌다면 점수가 높아도 `판단 보류`하고 더 가까운 사진이나 다른 각도의 사진을 요청한다. 목격 시각과 위치 사이의 이동 거리도 계산해 설정한 이동 속도 안에 있는지 확인한다. 좌표가 없어도 장소 이름, 마지막 확인 장소와의 대략 거리, 방향을 사용해 시간순 장소 경로를 제공한다. 보호자가 원본을 확인한 제보만 경로와 다음 수색 범위 계산에 사용한다.

## 핵심 사용자 흐름

1. 보호자가 반려동물 기준 사진 1~5장과 특징을 등록한다.
2. 마지막 확인 시각을 입력하고 공개 가능한 위치 좌표는 선택 입력한다.
3. 시민 목격 사진·시각·선택 위치·관찰 특징을 여러 건 등록한다.
4. 앱이 사진 특징, 사람의 관찰 특징, 이동 가능성을 분리해 비교한다.
5. `우선 확인`, `추가 확인`, `판단 보류`, `이동 경로 재확인`, `우선순위 낮음`으로 정리한다.
6. 보호자가 원본을 보고 신뢰할 제보를 직접 체크한다.
7. 확인된 제보의 시간 순 경로와 보수적인 예상 수색 중심·반경을 본다.

## 차별점

- 하나의 유사도 숫자만 제시하지 않고 근거와 반대 근거를 함께 보여준다.
- 정보가 부족하면 자동으로 확정하지 않고 판단을 보류한다.
- 사진이 닮아도 시간상 이동하기 어려우면 별도로 경고한다.
- AI가 아닌 보호자·구조 담당자가 경로에 포함할 제보를 최종 선택한다.
- 외부 API와 모델 다운로드 없이 노트북 CPU에서 시연할 수 있다.

## 첫 시제품의 기술

- Streamlit 다중 입력 UI
- React 기반 반응형 웹 UI와 브라우저 Canvas 분석
- OpenCV HSV 색 분포, 윤곽 방향, 저해상도 지각 패턴
- 선명도·밝기·대비 기반 사진 분석 신뢰도
- 사람이 기록한 털 색·얼굴 무늬·귀·체형·꼬리 비교
- Haversine 직선거리 또는 사용자 입력 대략 거리와 시간 차 기반 이동 필요 속도
- 좌표 없는 시간순 장소 경로와 방향·반경 기반 수색 참고 영역
- 좌표를 선택 입력한 경우의 지도 참고

Python/Streamlit 버전은 핵심 계산 검증과 로컬 시연에 사용하고, `web/` 버전은 설치 없이 체험하는 공개 시연 화면으로 사용한다. 웹 버전도 사진을 서버 파일로 저장하지 않으며 결과를 동일 동물 확률로 표현하지 않는다.

## 중요한 한계

- 동물 영역을 자동 분할하지 않아 배경색이 사진 특징에 영향을 준다.
- 귀·꼬리·얼굴 무늬는 현재 사람이 입력한 관찰값을 비교한다.
- 지각 패턴은 같은 개체의 다른 각도에서 크게 달라질 수 있다.
- 이동 가능성은 직선거리이며 실제 도로·하천·울타리를 반영하지 않는다.
- 좌표를 생략하면 사진·관찰 특징만 비교하고 이동 가능성과 경로는 계산하지 않는다.
- 예상 수색 중심은 정확한 위치 예측이 아니다.
- 동일 동물 여부를 확정하거나 확률로 제공하지 않는다.

## 테스트 기준

- 동일한 가상 동물의 조명 변화 사진은 다른 동물보다 우선 검토된다.
- 흐리고 일부만 보이는 사진은 충분한 점수가 있어도 판단 보류된다.
- 짧은 시간에 먼 거리를 이동해야 하는 제보는 경로 재확인으로 표시된다.
- 털 색·귀·꼬리 등 여러 특징이 다르면 불일치 근거가 표시된다.
- 위경도 거리와 예측 함수의 경계값을 단위 테스트한다.
- Streamlit 앱이 데모 기본 상태에서 예외 없이 열린다.
